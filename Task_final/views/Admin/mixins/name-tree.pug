//- mixin name-tree(items,parent_id)
//-     each item in items 
//-         span(item.id === parent_id? true : false) #{item.title}
//- mixin select-tree(items, parent_id="")
//-     each item in items 
//-         option(
//-             value=item.id
//-             selected=(item.id === parent_id? true : false)
//-             ) #{prefix}#{item.title}
//-             if item.children && item.children.length > 0
//-                 +select-tree(item.children, level + 1, parent_id)

mixin name-tree(items,leve=1, parent_id="")
    each item in items 
        if (item.id === parent_id)
            div(value=item.id) #{item.title}
        if item.children && item.children.length > 0
            +name-tree(item.children, level + 1, parent_id)